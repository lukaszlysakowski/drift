# Drift — SDD Progress Ledger

## Timeout-resilience protocol (binding)
- Implementers: commit incrementally after each step; create task-N-report.md at task START and update as you go.
- Controller: a tiny result or "session limit" text = limit-killed subagent. Do NOT re-dispatch until reset; salvage from git + working tree first.
- Fallback: CONTROLLER INLINE with explicit ledger attribution.
- Final review: fresh independent subagent (most capable model), zero deference to inline work.

## Task log
Task 1: complete (commits 32855bf..44eed77, verify 6/6, review clean incl browser load; canvas-container id correct, CSS byte-identical to Interference, renderAll index-based). NOTE: brief prose note still mentions indexOf (stale from plan edit) — code is correctly index-based; harmless. Launch config drift:3464 added by controller.
Task 2: complete (commits a0805f6..68ab3f2, verify 4/4, review clean; verbatim). Divergence-free foundation verified: baseField = curl of scalar potential, tested pre-normalization at <2% relative divergence — confirms all pooling is from feedback not field artifact.
Task 3: complete (commits 2c59fdf..8e9e008, verify 5/5, review clean after fix). PLAN BUG (mine) caught + fixed: implementer initially made depSmooth 10 passes to satisfy a gradient test that sampled ~7.5 cells from the pile; reviewer confirmed root cause was the mis-located test probe, not the smoothing. FIX: reverted depSmooth to spec-faithful SINGLE [1,2,1]/4 pass; moved test probe to 1190,1000 (~1.5 cells, gx=0.061). depSmooth is now single-pass per spec ('light 3-tap blur, not single-cell aliasing').
Task 4: complete (commits 63e37fd..5ebf8aa, verify 4/4, final review clean). Implementer correctly refused to over-fit and reported a test/code conflict (good protocol adherence). TWO fixes, both adjudicated sound: (A) PLAN BUG (mine) — divert guard raised 1e-9→1e-6 to implement spec's '|g|≈0' clause vs FD-noise floor (real gradients ~1e-4+); also prevents spurious steering at symmetric density extrema in PRODUCTION, not just the test. (B) same mis-located-probe class as Task 3 — divert test pile+probe moved to splat(900,900)/sample(890,900) (~1.5 cells); assertion unchanged (perp>1e-3), still genuine (perp≈1.5). bentField otherwise verbatim.
Task 5: complete (commits d2534f3..a5d200f, verify 9/9 incl channelization Strong>Off + determinism; ~7min runtime). Implementer again correctly reported a conflict instead of over-fitting (region-bounds). THREE fixes, all adjudicated: (1) PLAN BUG — region test had asymmetric tolerance (−1px low/+30px high); advect pushes one exit vertex up to one RK2 step (~6-11px w/ divert) past ANY boundary → symmetric ±35px MARGIN. (2) verbatim confirmed for runWaves/advect/waveStarts/inRegion/sumArr + constants. (3) SPEC-CONTRACT FIX (mine) — original runWaves splatted per-particle mid-wave while bentField read live state.dep → intra-wave order-dependence, which the user explicitly REJECTED when choosing per-wave-frozen over per-step-live. Fixed with two-phase wave (advect ALL on frozen field, THEN splat all). Now genuinely frozen per wave. NOTE for final review: verify runtime ~7min (many Med/Full regenerates) — candidate to slim later; not blocking. Dead vars beforeSum/prevSum in runWaves (harmless, from brief).
Task 6: complete (commits b4d775d..6e724d5, verify 6/6, classifyPaths verbatim-correct). SUBTLE PHYSICS/METRIC saga: implementer correctly reported a conflict — 'heavy fraction rises Off→Strong' FAILED inverted (Off 44%, Strong 5% heavy). Deep investigation (probe, not weakening) → H1 CORRECT EMERGENT PHYSICS: channelization concentrates deposition, raising depMax, which normalizes tributary D̂ down, so the heavy FRACTION correctly FALLS (few dense trunks + many thin tributaries = braiding); field is HEALTHY (50% mass over ~20% of cells, NOT collapsed). classifyPaths code is right; my TEST + SPEC item 5 had the backwards assumption. FIRST fix attempt introduced a fragile 'max/median totalD skew' invariant that a fixer then OVER-FIT (flipped rises→falls to match data — caught it). CORRECTED: dropped the fragile count-dependent skew check entirely (channelization is robustly owned by Task 5's raw-dep max/mean test); Task 6 now tests CLASSIFICATION ONLY (6 checks incl 'red channel path is heavy-class'). Spec item 5 corrected to describe the real invariant (raw-dep concentration + heavy-fraction-falls-is-expected). HEAVY_THRESH=0.28 unchanged; heavy/light split ratio is a browser-gate aesthetic knob.
Task 7: complete (commits bddbf50..e9073e7, verify 14/14, review clean; code verbatim). 14-vs-12: my prose undercounted the 5-label loop (5 checks, not 1); nothing added/dropped. Channel-exclusion + red-only-in-Channel + Channel-has-one-path all sound. Minor: report header said 'In progress' (cosmetic).
Task 8: complete (commits ce24d26..607b1bc, verify 8/8, review clean; index.js untouched). Implementer initially mislabeled correct normalization physics as an 'engine defect' but caught+corrected in fix pass. TWO fixes: (A) PLAN BUG (mine) — integration channelization check 'strong.heavy > off.heavy' was the SAME backwards assertion as Task 6; corrected to 'strong.heavy < off.heavy' (heavy-class count FALLS under Strong; off=3185 vs strong=344, robust ~9× gap; consistent w/ Task 6 physics). (B) HARNESS-CALIBRATION (mine) — soak Med/Full 30s bound was unrealistic for the Node vm-stub (~20× slower than browser). MEASURED BROWSER PERF: default Med/Light ≈ 6s (5400 paths), Med/Full ≈ 14s — acceptable UX for a deliberate sim-based maker. Recalibrated soak to Low/Full, 100s Node bound (still exercises 14-wave settle across 5 seeds; asserts paths>0/verts>1000/channelIdx>=0). README got a Performance note. 'expect 9' in plan was my miscount — script always had 8 checks, none dropped. Engine NOT optimized (6s default acceptable); base-field-grid-cache is a documented future speedup option if wanted.

## Final review (fresh independent opus, zero deference): READY
Confirmed the FINAL state is coherent, not a patchwork, despite the long fix history. Fast harnesses re-run: task1 6/6, task2 4/4, task3 5/5, task4 4/4, task7 14/14 (tasks 5/6/8 SLOW — verified via code+ledger+Low-count probe: 9/9, 6/6, 8/8). Key confirmations: (1) two-phase wave GENUINELY freezes state.dep during a wave's advection (Phase 1 advect-all reads only depAt, Phase 2 splats, then smooth/norm) — the frozen-field contract the user chose is honored; (2) determinism airtight (Math.random only at seed/randomizeAll; per-wave seededRng; wobble reseeds in-render only, never perturbs path set); (3) divert 1e-6 + single-pass depSmooth present & spec-faithful; (4) pass table exact, red only in Channel, channel excluded from ink; (5) classification physics TRUE and self-consistent (raw-dep concentrates AND normalized heavy-fraction falls — two metrics both true, not a contradiction); (6) no quadratics (renderAll/classifyPaths/buildSVG all linear); browser ~6s/~14s inherent sim cost. RED CHANNEL adjudged acceptable aesthetic (argmax total-traversed-density faithfully implements the spec; reads as a short curl in the deepest pocket rather than a long trunk — user-tunable via a length-weighted selection if wanted, NOT a defect). Minors fixed CONTROLLER INLINE: removed dead vars prevSum/beforeSum + wasted per-wave sumArr(before) call in runWaves (determinism re-confirmed PASS). Left non-blocking: slow "fast" harnesses (task2/4/7 now call regenerate); JetBrains Mono CDN font in index.html (falls back to monospace; SVG exports self-contained). Build ran the timeout-resilience protocol; multiple plan-bug fixes but ZERO limit-kill activations.

## Post-review tune (user request): prominent red channel
User asked to make the red main channel more prominent. Root cause (per final review): argmax(TOTAL traversed density) rewards DWELLING — a curl trapped in the single densest vortex racked up density without spanning. CHANGED selection to argmax(meanD · bbox-diagonal): dense AND spanning, so a long trunk that crosses the page beats a trapped curl. Also bumped weight: canvas red 2.4→3.8, SVG Channel pass 1.1→1.7. Verified in browser (seed 404 Strong/Full): channel now spans 1802px = 64% of page diagonal (was a short curl), reads as a clear trunk threading between vortices. Updated: spec §Rendering red-channel def + pass table (1.1→1.7); task-6-verify 'channelIdx == argmax(totalD)'→'argmax(meanD·span)' and 'red channel is heavy-class'→'red channel spans >30% of page diagonal'. Task 6 verify 6/6, Task 7 14/14, determinism intact. No console errors.
